name: Build Release

on:
  push:
    branches:
      - master

jobs:
  release_mac:
    name: Release Mac App
    runs-on: macos-10.15

    steps:

      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Yarn
        run: npm i -g yarn@latest

      - name: Install Dependencies and Build App
        env:
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
        run: echo `ls` && yarn install && yarn build && yarn publish:mac

  release_linux:
    name: Release Linux App
    runs-on: ubuntu-20.04

    steps:
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Yarn
        run: npm i -g yarn@latest

      - name: Install Dependencies and Build App
        env:
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
        run: echo `ls` && yarn install && yarn build && yarn publish:linux
