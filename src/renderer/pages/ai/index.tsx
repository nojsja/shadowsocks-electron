import React from 'react';
// import { useTranslation } from 'react-i18next';

// eslint-disable-next-line react/prop-types
const List = React.memo(function ReleaseNodes() {
  // const { t } = useTranslation();

  return <div>1</div>;
});

export default List;
